# Extractor de allocations de región
# Equivalente a: src/extractors/moneda/load_allocations.py
# TODO: Implementar carga y cruce de allocations_nuevas filtradas por classif == 'region'

def load_allocations_region(df_instruments):
    """
    Carga y cruza allocations nuevas (classif == 'region') con df_instruments.
    Retorna: allocations_nuevas_region, allocations_antiguas_region
    """
    raise NotImplementedError("Pendiente de implementación para región")
